language: php

sudo: false

php:
    - 5.5
    - 5.6
    - 7.0

notifications:
    email: dev@anime-db.org

before_install:
    - 'if [[ "$TRAVIS_PHP_VERSION" != "7.0 ]]; then echo "extension=memcache.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini ; fi;'
    - 'if [[ -n "$GH_TOKEN" ]]; then composer config github-oauth.github.com ${GH_TOKEN}; fi;'
    - composer self-update
    - mkdir build

install:
    - composer install --prefer-dist --no-interaction --no-progress

script:
    - vendor/bin/phpunit

after_script:
    - vendor/bin/ocular code-coverage:upload --format=php-clover build/coverage-clover.xml
    - vendor/bin/coveralls -v -c .coveralls.yml
